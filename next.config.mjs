/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['saimfolio.vercel.app', 'images.pexels.com'], // Add your image domain here
    },
};

export default nextConfig;
